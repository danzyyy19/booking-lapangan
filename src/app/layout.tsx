import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Danzyy Field - Booking Lapangan Online",
  description: "Sistem booking lapangan olahraga online. Pesan lapangan futsal, badminton, basket, dan lainnya dengan mudah.",
  keywords: "booking lapangan, sewa lapangan, futsal, badminton, basket, olahraga",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="id" suppressHydrationWarning>
      <body className={`${inter.variable} antialiased`}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
